<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Tranning"
        xmlns:dxwui="http://schemas.devexpress.com/winfx/2008/xaml/windowsui" xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" x:Class="WPF_Tranning.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="567" Width="1249">

    <Grid>
        <!--체크박스 샘플 
            https://johnnblade.wordpress.com/2012/05/24/grid-control-gridcontrol-with-celltemplate-and-checkbox-or-checkedit-unchecks-or-checks-other-unchecked-checkboxes-when-scrolling-or-checkbox-comes-up-when-new-visual-rows-displays-devexpress-wpf/
            -->
        <!-- DB내용 View -->
        <dxg:GridControl x:Name="scoreListViewDB" AutoGenerateColumns="AddNew" HorizontalAlignment="Left" Margin="156,10,0,0" VerticalAlignment="Top" Height="323" Width="849" ItemsSource="{Binding SelectTable}">
            <dxg:GridControl.View>
                <dxg:TableView  ShowTotalSummary="True" Margin="0,3,8,55"/>
            </dxg:GridControl.View>

            
            <dxg:GridColumn FieldName="스코어 체크" x:Name="scorenumber" Width="100">
                
                <dxg:GridColumn.CellTemplate>
                    <DataTemplate>
                        <!--<CheckBox Name="chkSelect"  HorizontalAlignment="Center" IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DataContext.RowData.Row.MediaSelected}"  Checked="CheckEdit_Checked" Unchecked="CheckEdit_Unchecked" />-->
                        <CheckBox x:Name="checkbox" Click="chkSelect_Click" IsChecked="{Binding Score_id}"  Content="{Binding Score_id}"  HorizontalAlignment="Center" />
                    </DataTemplate>
                </dxg:GridColumn.CellTemplate>
            </dxg:GridColumn>

            <dxg:GridColumn FieldName="스코어 번호" Binding="{Binding Score_id}"/>
            <dxg:GridColumn FieldName="내용" Binding="{Binding Score}"/>
            
        </dxg:GridControl>
        <Button Click="valuebutton_Click" Command="{Binding CheckBinding}" x:Name="valuebutton" Content="DB바인딩" HorizontalAlignment="Left" Margin="181,432,0,0" VerticalAlignment="Top" Width="119"/>
        <Button Command="{Binding TestBinding}" x:Name="valuebutton2" Content="바인딩" HorizontalAlignment="Left" Margin="326,432,0,0" VerticalAlignment="Top" Width="119"/>




    </Grid>
</Window>
